<?xml version="1.0"?>
<record_update>
  <sys_module action="INSERT_OR_UPDATE">
    <sys_scope>a6e509aec341c650c9bcbe2bb001319c</sys_scope>
    <path>x_snc_sndp_twitch/sndp-twitch/1.0.0/src/todo.js</path>
    <external_source>false</external_source>
    <content><![CDATA[// TODO

// move chat sender to a bot account
    // pointsthingjr
    // pointsthinggr
    // pointymcpointface
    // servicenowbeepboop
    // servicenowdevpointgram
    // SNOWbot
    // PointsOnNow
    // pointSNOW
// record chatter presence
    // add confirmed_real to table
    // send rest message of chatter presence
    // we went with adding !points as the call but maybe just do any person chatting sends the call
// add confirmed troll -eric
// things you can do with points
// leaderboard
// move this to a markdown file dummy
// add source control before you do anything else you might regret
// add captions -chris
// stream interactions
    // stream avatars -ditziv
    // the more disruptive commands can cost points (but duplicate score) -ditziv
    // donut bounds instead of just scrolls
    // muppets critics gallery -eric+asteroid
    // !jumpscare -laszlo 

// BUGS

// weird delay/wrong person situation with !points
// why does !snap not return to scene]]></content>
  </sys_module>
</record_update>